# FIX: normalise flat Classic/Torus solution arrays before drawing.
# The daily JSON stores Classic/Torus solutions as flat n*n lists, while
# Cube uses nested layers. The renderer must accept both shapes.
